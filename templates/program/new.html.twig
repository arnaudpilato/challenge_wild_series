{% extends 'base.html.twig'%}

{% block body %}
    <h1>Ajouter un foutu programme</h1>
    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_widget(form) }}
    <input type="submit" value="Add Program">
    {{ form_end(form) }}
{% endblock %}